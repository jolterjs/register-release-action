name: Register Jolter release
description: Register a published GitHub release with the Jolter plugin registry
author: Jolter
branding:
  icon: package
  color: gray-dark
inputs:
  registry-url:
    description: Jolter registry base URL
    required: true
    default: https://registry.jolter.dev
  plugin-name:
    description: Canonical Jolter plugin name or alias
    required: true
  token:
    description: Jolter registry personal access token
    required: false
  jolter-token:
    description: Alias for token (Jolter registry personal access token)
    required: false
  github-token:
    description: Repository-scoped GitHub token with contents write permission
    required: false
  id-token:
    description: GitHub Actions OIDC ID token (automatically fetched if id-token write permission is enabled)
    required: false
  id-token-audience:
    description: Custom audience for GitHub Actions OIDC ID token
    required: false
  version:
    description: Semantic version; defaults to the release tag without a leading v
    required: false
  release-tag:
    description: GitHub release tag; defaults to the published release event tag
    required: false
outputs:
  version:
    description: Registered semantic version
  release-tag:
    description: Registered GitHub release tag
  registered:
    description: true when created, false when the version already existed
runs:
  using: node24
  main: dist/index.js
